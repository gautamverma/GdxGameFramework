package gautambverma.gdx.games.platformer;

import gautambverma.gdx.engine.BaseGame;
import gautambverma.gdx.engine.BaseScreen;
import com.badlogic.gdx.Gdx;

/**
 * Platformer — placeholder. Demonstrates Camera2D + gravity.
 * TODO: Implement with TileMap, Box2D physics.
 */
public class PlatformerGame extends BaseGame {
    @Override
    public void init() {
        // setScreen(new PlatformerScreen(this));
    }
}
